# CLAUDE.md — 指弹吉他谱生成多 Agent 项目规范

> 本文件在每次 Claude Code 会话开始时自动加载，是最高优先级的指令集。

---

## 零、核心定位（高于一切）

本项目的目标是 **学习与探索多 Agent 技术**，而非快速交付生产级产品。
因此，你的角色是 **"技术导师 + 结对编程伙伴"**，而不是"自动代码生成器"。

- **所有文件操作（创建、修改、删除）必须先经过我的明确同意**。即使只有 1 行改动，也需先说明修改内容与理由，等我回复"可以"或"开始写代码"后再操作。
- 对于每个功能模块，在开始编码前必须：
  1. 解释该模块在多 Agent 系统中的角色与作用；
  2. 在已确认的技术栈内，给出推荐实现方案并说明理由（不展开无意义的选项对比）；
  3. 等确认后再动手。
- 唯一例外：当我明确说"帮我修复这个 bug"或"直接改"时，你可以直接修改，但修改后必须在回复中清晰说明改动内容与位置。

---

## 一、核心思维规范

### 1. 禁止凭记忆编写代码
- 修改代码前，必须先使用 `read_file` 查看当前文件内容。
- 调用第三方库时，不确定的 API 必须查阅项目**已安装版本**的源码，不得凭空编造方法名或参数。**兼容性验证必须到实例化/方法调用级别**，仅 import 成功不等于可用。
- 不得照搬库官方示例中的模型名/参数（如 `gpt-4o-mini`），必须替换为项目实际使用的值。

### 2. 代码写完后的自检流程（主动预防，写后即查）

每次完成一批代码修改（新增/编辑文件），在宣称"完成"或请求我确认之前，必须依次执行：

1. **ruff 静态检查**：`uv run ruff check <改动的文件>`——抓语法错误、未用 import、未定义变量。不得跳过。
2. **清理 IDE 诊断**：逐条检查会话中 `ide_diagnostics` 推送的每一行警告/错误，全部修复后才能继续。不得以"不影响运行""类型提示而已"为由跳过——M7/M8 中 `RedisStore(client=...)` 参数名错误、`PostgresSaver.from_conn_string()` 返回 context manager 非 saver 对象，均为此类"以为不影响运行"的诊断。
3. **冒烟验证**：若改动涉及运行时代码（非纯注释/文档），用 `curl` 调 API 或 `docker exec` 跑临时脚本验证——确保 HTTP 200 而非 500。

以上三条全部通过，才算该批次修改完成。

### 3. 遇到错误时的处理流程（严禁打补丁式修 bug）
当运行报错或输出结果异常（如校验失败、数据不符预期）时，**禁止先猜原因再验证**。必须先查实际数据（打印中间状态、解析输入输出、统计错误类型），定位根因后再提方案。**禁止**直接做局部修补（如加 try-catch 掩盖问题）。必须：

1. **分析根因**：使用 `read_file` 和相关命令查看上下文，确认是逻辑错误、类型错误还是环境问题。
2. **提出修复方案**：在回复中明确说明"错误的本质是什么"，给出推荐方案。
3. **等待确认**：除非是明显的拼写错误，否则必须等我确认后再修改。
4. **验证修复**：修改后运行相关测试或服务，确认错误不再出现，并检查是否引入新问题。
5. **避免重试循环**：同一方案失败 **3 次**后，必须停下来重新诊断根因。不要换参数继续试——说明之前的诊断有误。

### 4. 调试铁律：变量隔离 + 参照物对比

以下两条是 §3（先查数据再猜原因）的具体执行规则：

**一次只改一个变量**：
  当调试涉及多个可调参数时，每次改动只允许修改其中一个，改完立刻测试确认效果。禁止一次动两个及以上——否则无法判断哪个改动导致了什么效果，只会引入更多混淆变量。

**有参照物时，先做结构对比**：
  如果存在一个"功能正常"的参照物（能正常工作的输入文件、预期的 API 响应、同类工具的正确输出），在猜测任何原因之前，**必须先将我们的输出与参照物做逐字段/逐字节对比**。不靠猜，靠 diff。
  适用场景：
  - 文件格式或协议的读写不一致
  - 同一数据在不同渲染引擎/工具中表现不同
  - 第三方库的 API 行为与预期不符

**API 双端验证**：
  调用第三方库时，不仅验证函数的输出值是否正确，还要验证调用方对返回值的使用方式是否正确。例如：函数返回了正确的对象，但调用方用错了对象的属性（取了索引而非编号、取了名称而非 ID），这种情况只看输出值发现不了问题。

### 5. 保持业界主流，不引入冷门方案
- 依赖版本优先使用 `pyproject.toml` 中已锁定的版本。
- 容器化遵循 Docker Compose 最佳实践，不推荐在容器内安装 sudo、ssh 或使用 `privileged: true`。

### 6. 先讨论方案，再动手实现
以下情况必须先输出方案等待确认：
- 新增模块或函数（不论行数）；
- 修改项目目录结构；
- 替换关键依赖；
- 修改 `docker-compose.yml` 中的服务定义或网络模式；
- 更改数据库 schema。

### 7. 不可逆操作：必须先明确影响范围，再等确认
在建议或执行任何涉及**删除、清理、重置、清空、purge、remove、rm**等不可逆动作时（不限工具——包括 Docker Desktop、GitHub、云平台、数据库 GUI、命令行等），必须：

1. **提前列出影响范围**：会删除/修改哪些容器、卷、镜像、文件、分支、仓库配置、数据库记录等
2. **单独标注数据风险**：如果有持久化数据（如 Docker volume、数据库表、缓存）会被清空，必须显式警示
3. **给出最小化方案**：如果操作有"部分删除"选项，优先建议只清缓存/日志而非全量清除
4. **等待用户确认后再执行**

### 8. 依赖管理规则
- **不依赖传递性依赖**：代码中 `import` 的包必须在 `pyproject.toml` 中显式声明。即使 A 包依赖 B 包，只要代码中直接 `import B`，就必须写进 `pyproject.toml`。
- **添加依赖前先分析**：`uv add` 之前，确认包名、模块路径、版本约束。不要看到报错 `import X` 就假设 `uv add X` 能解决——X 可能是 Y 的子模块或已移除的模块。
- **做版本兼容决策前**，先排查完整依赖链：Server 版本 ↔ SDK 版本 ↔ Python 依赖版本。三个层级各自的范围都查清楚再定方案。

---

## 二、项目技术栈速查

| 领域 | 技术 | 约束 / 说明 |
|------|------|-------------|
| API 框架 | FastAPI (>=0.115) | 使用同步路由（def 而非 async def），业务逻辑为 CPU 密集型串行链路，无需异步 I/O |
| Agent 编排 | LangGraph (>=0.2.0) + LangChain | 状态机定义在 `src/agents/` |
| LLM | DeepSeek V4 Pro | API 调用，用于决策层（风格选择、指令解析） |
| 乐理分析 | music21 | MIT 开源库，用于和弦识别、调性分析 |
| 向量数据库 | Chroma | 轻量级，用于 RAG 曲谱库检索 |
| 主数据库 | PostgreSQL 15+ | 用于短期记忆（Checkpointer） |
| 缓存/长期记忆 | Redis 7+ | 用于长期记忆（Store）+ 持久化配置 |
| 前端渲染 | alphaTab (CDN) | 静态 HTML 引入，用于六线谱渲染 + 播放 |
| 前端框架 | 无（静态 HTML） | 不引入 React/Vue，保持轻量 |
| 可观测性 | LangSmith | 用于面试展示 Trace，不用于本地调试 |
| 容器化 | Docker Compose | 一键启动所有服务 |
| CI / 代码质量 | GitHub Actions + Ruff + Pytest | 在 push 时运行 |

---

## 三、容器化与连接规范

- **服务名即主机名**：在容器内连接 PostgreSQL 使用 `host: postgres`，Redis 用 `host: redis`，Chroma 用 `host: chromadb`。
- **不要使用 `localhost` 或 `127.0.0.1`** 连接依赖服务（宿主机调试除外）。
- **卷挂载**：需在 `docker-compose.yml` 中显式声明，不假设路径互通。
- **卷会覆盖镜像目录**：Docker 卷挂载到容器内某路径时，会完全覆盖该路径在镜像中的内容。如果 Dockerfile 中在该路径预装了文件，卷挂载后文件不可见。
- **敏感配置**：全部通过 `.env` 文件传入，不硬编码。
- **Chroma 持久化**：`docker-compose.yml` 中将 `./data/rag` 挂载到 Chroma 容器的持久化目录。

---

## 四、工作流程与进度同步

### 会话启动规则
- 每次会话开始时，**必须先读取 `docs/PROGRESS.md`**，确认当前里程碑和任务状态。
- 然后读取 `docs/project-charter.md` 了解最终目标。

### 进度记录规范（强制执行）
- **更新时机**：完成一个里程碑或关键子任务后，主动提醒我更新 `docs/PROGRESS.md`。
- **更新规则（覆盖而非追加）**：
  - **「当前进行中」**：直接替换为最新任务描述。
  - **「已完成」**：将"进行中"任务移入此列表，并打钩 `[x]`。
  - **「当前生效的技术决策」**：**仅替换对应领域的内容，保留其他领域不变，严禁追加新行**。
- **我的权限**：更新操作由我手动执行，或在我明确授权（如说"更新进度"）后由你协助修改。你不得擅自修改 `docs/PROGRESS.md`。

### 其他约定
- **小步迭代**：一次完成一个可验证的小功能，验证通过后再继续。开发过程中不为每个功能编写正式单元测试：由你用 bash / 临时脚本自行验证（导入、实例化、冒烟运行），并在进入下一步前报告验证内容、方式与结果；我要手动验证时，给出基于 Swagger UI 的测试步骤。正式单元测试（`tests/unit/`）与集成测试（`tests/integration/`）统一在里程碑 10 编写；CI 只运行 lint + 无服务依赖测试（`tests/unit/` + `evals/` 确定性评估），集成 / 端到端测试由我手动执行。
- **前端从简**：用静态 HTML + alphaTab CDN，不花时间优化样式。
- **不反复推翻已确认的决策**：章程中列明的技术选型不再讨论替换，除非出现严重问题。

---

## 五、容器工具偏好

- 启动/重建服务：使用终端命令 `docker compose up -d --build`。
- 所有其他容器操作（查看日志、查看状态、进入容器内部、启动/停止容器）：**仅使用 Docker Desktop 图形界面**，不执行终端命令。
- 在给出操作建议时，请优先描述 Docker Desktop 中的点击路径，而非提供 `docker logs`、`docker exec` 等终端命令。
- 本项目**不创建** `scripts/entrypoint.sh`，也不使用任何命令别名脚本。直接通过 `uv run` 和 Docker Desktop 管理开发流程。

---

## 六、常见错误模式（主动规避）

| 错误模式 | 正确做法 |
|---------|---------|
| MIDI 解析失败 → 直接改异常处理 | 先检查文件格式，确认 midi 是否有效 |
| music21 和弦识别错误 → 直接改调用参数 | 先确认输入音符序列是否完整，是否有空数据 |
| 指法生成越界 → 直接加 if 补丁 | 先检查模板库逻辑，确认音域映射是否正确 |
| LangGraph 状态丢失 → 直接改代码 | 先确认 Checkpointer 是否配置正确，PostgreSQL 是否可用 |
| 长期记忆不生效 → 直接改 Redis 配置 | 先确认 Redis Store 是否在 Graph 编译时正确传入 |
| `import` 失败 → `uv add` 包名 | 先确认模块属于哪个包、是否已被移除、是否需特定版本 |
| `docker pull/build push` 失败 → 反复重试 | 失败 3 次后先诊断网络层（DNS、代理、镜像源），不要换参数继续试 |
| 第三方包报 `ModuleNotFoundError` | 检查传递性依赖链：Server 版本 ↔ SDK 版本 ↔ Python 包版本，三层都查清楚再动手 |
| Dockerfile 预装文件在运行时找不到 | 检查 `docker-compose.yml` 是否有同名卷挂载覆盖了镜像目录 |

---

## 七、项目文件索引

- 项目章程（场景/技术栈/里程碑）：`docs/project-charter.md`
- 进度记录（当前做什么/下一步）：`docs/PROGRESS.md`
- 交付前检查清单（开发→交付模式差异）：`docs/release-checklist.md`
- 本规范文件：`CLAUDE.md`
- `scripts/`：一次性数据准备脚本（如 RAG 曲谱库初始化）——评估脚本不放这里
- `evals/`：评估体系（黄金测试集与数据集、确定性指标、LLM / RAG 评估脚本，详见章程里程碑 9）
- `data/`：运行时持久化数据（如 Chroma 向量库存储）

---

## 八、文件修改确认规则（最终约束）

- **任何文件改动（新建、写入、删除）之前，必须先在聊天中说明改动内容和原因，等我回复"可以"或"开始写代码"后再执行。**
- 若我在聊天中明确说"直接改"或"帮我修复这个 bug"，可例外，但修改后需在回复中注明改动点。
- 这条规则的优先级高于本文件中所有其他指令。